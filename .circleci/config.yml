version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:2024.11
    steps:
      - checkout
      - run:
          name: Test build
          command: |
            echo "Hello from CircleCI"
            ls -la

workflows:
  workflow:
    jobs:
      - build